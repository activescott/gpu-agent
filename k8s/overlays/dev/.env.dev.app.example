# Database
POSTGRES_PRISMA_URL=postgresql://gpu_agent:password@db:5432/gpu_agent?schema=public&pgbouncer=true&connect_timeout=15
POSTGRES_URL_NON_POOLING=postgresql://gpu_agent:password@db:5432/gpu_agent?schema=public&connect_timeout=15

# Domain
PUBLIC_DOMAIN=localhost:3000

# eBay API - Get from https://developer.ebay.com/my/keys
EBAY_CLIENT_ID=your_ebay_client_id
EBAY_CLIENT_SECRET=your_ebay_client_secret
EBAY_ENVIRONMENT=SANDBOX
EBAY_AFFILIATE_CAMPAIGN_ID=your_campaign_id

# PostHog Analytics
# get from https://us.posthog.com/project/43454/settings/project
PUBLIC_POSTHOG_KEY=your_posthog_key
PUBLIC_POSTHOG_HOST=http://localhost:3000/a

# IndexNow API Key - Generate with: packages/indexnow-notifier/scripts/generate-key.sh
INDEXNOW_API_KEY=dev-test-key-12345678

# Google Indexing API (optional) - See README.md for setup instructions
# Option 1: Base64-encoded JSON key file (recommended for k8s secrets)
# Get from Google Cloud Console > IAM & Admin > Service Accounts > Keys > Add Key > JSON
# Encode with: base64 -i service-account.json
# GOOGLE_SERVICE_ACCOUNT_JSON=base64-encoded-json-here
#
# Option 2: Individual fields (alternative)
# GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@project.iam.gserviceaccount.com
# GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Optional: Control which providers to use (default: all configured)
# ENABLED_PROVIDERS=indexnow,google

# Oxylabs Residential Proxy - Used by amazon-searcher for scraping Amazon search results.
# Create/retrieve credentials at https://dashboard.oxylabs.io/en/overview/RP/users
# The username is the sub-user name (not your account email).
OXYLABS_USERNAME=your_oxylabs_username
OXYLABS_PASSWORD=your_oxylabs_password

# Amazon Associates Affiliate Tag - Appended to product URLs for affiliate revenue.
# Get from https://affiliate-program.amazon.com/ > Account > Manage Your Tracking IDs
# Required in production (logs error on startup and warning per search if missing).
AMAZON_AFFILIATE_TAG=yourtag-20

# Internal pages auth (Basic Auth) - leave empty to disable auth in dev
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
