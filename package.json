{
  "name": "gpu-agent",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "lint-fix": "turbo run lint-fix",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "watch": "turbo run watch",
    "clean": "turbo run clean",
    "dev": "./scripts/dev",
    "dev:reset": "./scripts/reset-dev",
    "dev:logs": "./scripts/logs-dev",
    "dev:reseed": "./scripts/reseed",
    "prepare": "husky install"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@10.2.4",
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "dependencies": {
  }
}
